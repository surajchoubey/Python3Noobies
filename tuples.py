numbers = (1,2,3)
#print(numbers.count())
#print(numbers.index()) only count and index functions support tuples learn them later how to use them
#numbers[0]=10 this is an invalid move don't try to execute it cause it is an error
print(numbers[0])